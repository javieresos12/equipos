<%= form_with(model: asignacione, local: true) do |form| %>
  <% if asignacione.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(asignacione.errors.count, "error") %> prohibited this asignacione from being saved:</h2>

      <ul>
      <% asignacione.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field row col-sm-4">
    <%= form.label :persona_id %>
    <%= form.text_field :persona_id, id: :asignacione_persona_id, class:"form-control" %>
  </div>

  <div class="field form-group row">
    <label class="col-2 col-form-label"><%= form.label :fecha %></label>
    <div class="col-10">
      <%= form.date_select :fecha, id: :asignacione_fecha, class:"form-control"  %>
    </div>
  </div>

  <div class="field  row col-sm-4 "> 
    <%= form.label :idEstadoPersonaEquipo %>
    <%= form.number_field :idEstadoPersonaEquipo, id: :asignacione_idEstadoPersonaEquipo, class:"form-control" %>
  </div>

  <div class="field row col-sm-4">
    <%= form.label :eliminado %>
    <%= form.number_field :eliminado, id: :asignacione_eliminado, class:"form-control" %>
  </div>

  <div class="actions">
    <%= form.submit "Guardar", :class=>"btn btn-secondary"%>
  </div>
<% end %>
